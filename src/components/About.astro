---

---

<script>
  const $ = (id: string) => document.getElementById(id);
  let texto = "Desarrollador Web";
  let i = 0;

  function escribirTexto() {
    if (i < texto.length) {
      const letteringElement = $("lettering");
      if (letteringElement) {
        letteringElement.innerHTML += texto.charAt(i);
      }
      i++;
      setTimeout(escribirTexto, 50);
    } else {
      setTimeout(borrarTexto, 2000);
    }
  }

  function borrarTexto() {
    if (i > 0) {
      const letteringElement = $("lettering");
      if (letteringElement) {
        letteringElement.innerHTML = texto.substring(0, i - 1);
      }
      i--;
      setTimeout(borrarTexto, 50);
    } else {
      setTimeout(escribirTexto, 0);
    }
  }

  escribirTexto();
</script>

<section id="sobre-mi" class="py-16">
  <div class="mx-full my-16 px-4 sm:px-6 lg:px-8">
    <div class="flex flex-row justify-between gap-8 items-center">
      <div class="space-y-6">
        <h2 class="text-3xl font-bold text-gray-900">
          ¡Hola, soy <br /><strong>Diego Sepulveda</strong>!
        </h2>
        <h3 id="lettering" class="typing-loop"></h3>
        <p class="text-lg text-gray-700 mb-4">
          Soy un desarrollador web con experiencia en el desarrollo de
          aplicaciones web. Mi pasión por el desarrollo web me ha llevado a
          crear sitios web y aplicaciones que facilitan la vida de los usuarios
          y mejoran la experiencia de los usuarios.
        </p>

        <!-- Hobbies section -->
        <div>
          <h3 class="text-xl font-semibold text-gray-900 mb-4">
            Mis pasatiempos
          </h3>
          <p class="text-gray-700">
            [Escribe aquí algunos de tus pasatiempos e intereses personales]
          </p>
        </div>
      </div>
      <img
        src="/img/icon-diego.png"
        alt="Imagen de Diego Sepulveda Desarrollador Web"
        class="[width:40%] h-auto object-cover hover:scale-105 transition-transform duration-300 transform group-hover:scale-105"
      />
    </div>
  </div>
</section>
